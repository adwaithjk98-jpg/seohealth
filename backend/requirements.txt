fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
selenium==4.27.1
# Wired in (USE_UNDETECTED_CHROMEDRIVER=true) for future stealth work
# on Google Maps' review-count strip — stock undetected-chromedriver
# alone doesn't solve it on either headless or visible Chrome, so the
# default is plain Selenium. Pair this with a persistent profile and/
# or residential proxy when reviving the work.
undetected-chromedriver==3.5.5
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
resend==2.4.0
psycopg[binary]==3.2.3
redis==5.2.1
rq==1.16.2
rq-scheduler==0.13.1
# Phase 4 — competitor insights LLM phrasing. The service degrades to a
# deterministic sentence when ANTHROPIC_API_KEY is unset, so the dep is
# pulled in but the runtime tolerates it being missing too.
anthropic==0.42.0

# HTTP rate limiting + error monitoring (added 2026-06-08)
slowapi==0.1.9
sentry-sdk[fastapi]==2.20.0

# Web Push (VAPID) — scheduled-audit-done + competitor-moved notifications.
# Sending is gated on VAPID_* config, so the runtime tolerates this being
# unconfigured; pulls in ``cryptography`` (used by scripts/gen_vapid_keys.py).
pywebpush==2.0.0
