fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
selenium==4.27.1
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
resend==2.4.0
psycopg[binary]==3.2.3
redis==5.2.1
rq==1.16.2
rq-scheduler==0.13.1
# Phase 4 — competitor insights LLM phrasing. The service degrades to a
# deterministic sentence when ANTHROPIC_API_KEY is unset, so the dep is
# pulled in but the runtime tolerates it being missing too.
anthropic==0.42.0
