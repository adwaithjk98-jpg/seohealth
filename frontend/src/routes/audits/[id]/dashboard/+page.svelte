<!-- This route only redirects (see +page.js): /businesses/{id} is the one
     canonical dashboard. The component exists so the route stays in the
     manifest; it renders for at most a frame while the redirect resolves. -->
<p class="text-sm text-canvas-muted">Taking you to your health check…</p>
